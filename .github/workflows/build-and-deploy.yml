name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: |
           npm install -g npm@latest
           npm install --legacy-peer-deps
           npm install -g pnpm --legacy-peer-deps
      - name: Bu
        env: |
          ACCESS_TOKEN: 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
          BRANCH: main
          BUILD_SCRIPT: npm run build

  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Generate deployment package
  #       run: zip -r deploy.zip . -x '*.git*'

  #     - name: Deploy to EB
  #       uses: einaregilsson/beanstalk-deploy@v21
  #       with:
  #         aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         application_name: mononoke-site
  #         environment_name: mononoke-site-dev
  #         version_label: 0.0.5
  #         region: ${{ env.AWS_REGION }}
  #         deployment_package: deploy.zip
